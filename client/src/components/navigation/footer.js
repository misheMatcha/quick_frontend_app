import React from 'react';

const Footer = () => {
  const footerBar = () => {
    return <div className="container footer">
      footer
    </div>
  };

  return footerBar();
};

export default Footer;